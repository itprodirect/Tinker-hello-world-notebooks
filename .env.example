# Copy this file to `.env` and fill in each value before running the notebooks.

# Your personal or workspace API key from the Tinker dashboard.
# https://tinker.thinkingmachin.es/ (Profile → API Keys)
TINKER_API_KEY=your-api-key-here

# Optional: specify a key slot if your org issues numbered credentials.
# Leave commented unless instructed otherwise by your admin.
# TINKER_KEY_NUMBER=0
